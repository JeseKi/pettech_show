version: '3.8'

services:
  app:
    build:
      context: .
      args:
        MIRROR_MODE: ${MIRROR_MODE:-auto}
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data:rw
      - ./logs:/app/logs:rw
